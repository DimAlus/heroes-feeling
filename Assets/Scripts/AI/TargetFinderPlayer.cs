using UnityEngine;

public class TargetFinderPlayer : TargetFinderBase {

  public override Vector3 FindTarget(EAbilityTarget target, EAbilityTargetFinder finder) {
    return base.FindTarget(target, finder);
  }
}
